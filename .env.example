# Path to the subtitles root containing {lang}/{lang}/{lang}_metadata.json.
# - For the build script (npm run gen:index), unset defaults to ./fake-subtitles.
# - For docker compose, unset defaults to ./subtitles.
# Set it to wherever you extracted the Subscene Final archive, e.g.:
SUBTITLES_ROOT=./subtitles
